apiVersion: v1
kind: Service
metadata:
  name: vllm-qwen-service # Gate Service 调用的固定域名
spec:
  selector:
    app: vllm-qwen # 本地和 AWS 的 Pod 都必须带这个标签
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP